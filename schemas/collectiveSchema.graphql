# import TaskListingSchema from './taskListingSchema.graphql';
# import TaskDetailSchema from './taskDetailSchema.graphql';

type Query {
  taskListing(page: Int): [TaskListing]
}

type Mutation {
  taskDetail(id: Int): TaskDetail
}

schema {
  query: Query
  mutation: Mutation
}