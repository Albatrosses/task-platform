# import TaskListingSchema from './taskListingSchema.graphql';
# import TaskDetailSchema from './taskDetailSchema.graphql';

type Query {
  getTaskListing(page: Int): [TaskListing]
  getTaskDetail(id: Int): TaskDetail
}

type Mutation {
  UpdateTaskDetail: TaskDetail
}

schema {
  query: Query
  mutation: Mutation
}