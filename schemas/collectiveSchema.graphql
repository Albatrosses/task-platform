# import TaskListingSchema from './taskListingSchema.graphql';
# import TaskDetailSchema from './taskDetailSchema.graphql';

type Query {
  queryTaskListing(page: Int, status: Int, platform: [Int], reward: [Int], date: [String]): [TaskListing]
  queryTaskDetail(id: Int): TaskDetail
}

type Mutation {
  mutationTaskDetail(input: TaskDetailInput!): Boolean
}

schema {
  query: Query
  mutation: Mutation
}