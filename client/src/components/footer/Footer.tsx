import * as React from "react";
import styled from "styled-components";

export const Footer: React.FC<any> = () => {
  return <FooterrWrapper>Footer</FooterrWrapper>;
};

const FooterrWrapper = styled.div`
  flex: 0 0 40px;
`;
