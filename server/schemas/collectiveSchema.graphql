# import TaskListingSchema from './taskListingSchema.graphql';
# import TaskDetailSchema from './taskDetailSchema.graphql';

type Query {
  taskListing(page: Int, status: Int, platform: [Int], reward: [Int], date: [String]): [TaskListing]
  taskDetail(id: Int): TaskDetail
  heroImage: [HeroImage]
}

type Mutation {
  updateTaskDetail(input: TaskDetailInput!): Boolean
}

schema {
  query: Query
  mutation: Mutation
}